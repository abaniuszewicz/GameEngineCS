<UserControl
    x:Class="Engine.Input.DPad"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Engine.Input"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Viewbox Opacity="0.2">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>

            <Button x:Name="LeftButton" Content="&lt;" Grid.Row="1" Grid.Column="0" Click="LeftButton_OnClick"/>
            <Button x:Name="UpButton" Content="^" Grid.Row="0" Grid.Column="1" Click="UpButton_OnClick"/>
            <Button x:Name="RightButton"  Content="&gt;" Grid.Row="1" Grid.Column="2" Click="RightButton_OnClick"/>
            <Button x:Name="DownButton" Content="v" Grid.Row="2" Grid.Column="1" Click="DownButton_OnClick"/>

        </Grid>
    </Viewbox>
</UserControl>
